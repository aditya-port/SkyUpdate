# SkyUpdate
Designed a hybrid OLTP + analytical PostgreSQL schema with ingestion tracking and time-series storage. Built normalized tables and executed dynamic SQL analysis using joins, window functions, and conditional logic to support real-time insights without precomputed metrics.
